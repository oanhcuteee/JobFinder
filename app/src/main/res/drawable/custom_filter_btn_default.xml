<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/filter_border_btn_default"/> <!--Màu nền-->
    <corners android:radius="4dp" /> <!-- Độ cong của góc -->
    <size android:height="42dp" /> <!-- Kích thước của Button -->
    <stroke
        android:width="1dp"
        android:color="@color/filter_border_btn_default" />
    <padding
        android:left="8dp"
        android:top="5dp"
        android:right="8dp"
        android:bottom="5dp" /> <!-- Khoảng cách giữa viền và nội dung của Button -->
</shape>
